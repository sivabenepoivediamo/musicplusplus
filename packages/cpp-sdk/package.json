{
    "name": "cpp-sdk",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "cmake --preset default && cmake --build --preset default",
        "test": "echo \"Error: no test specified\" && exit 1",
        "clean": "cmake -E remove_directory build",
        "docs": "doxygen Doxyfile",
        "docs:clean": "cmake -E remove_directory docs"
    }
}